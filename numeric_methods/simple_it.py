import os
import numpy as np

def get_data() -> np.ndarray:
    '''
    Запрашивает у пользователя данные для системы уравнений.
    
    Returns:
        np.ndarray: Массив коэффициентов и свободных членов.
    '''
    n = int(input('Введите количество переменных --> '))  # Запрашиваем количество переменных
    os.system('cls')  # Очищаем консоль
    print(f'✅ Вы ввели {n}\n')  # Подтверждаем ввод пользователя

    array = []  # Инициализируем список для хранения уравнений
    for i in range(n):
        while True:
            # Запрашиваем ввод коэффициентов и свободного члена для текущего уравнения
            string = input(f'▶ Введите {n + 1} чисел через пробел для {i + 1}-го уравнения --> ')
            tmp = list(map(float, string.split()))  # Преобразуем ввод в список чисел
            if len(tmp) == (n + 1):  # Проверяем, что введено правильное количество чисел
                print('✅ Отлично, количество совпало')
                array.append(tmp)  # Добавляем уравнение в массив
                break
            else:
                print(f'❌ Ошибка: Вы ввели {len(tmp)}, но требуется {n + 1}')  # Сообщаем об ошибке

    num_arr = np.array(array, dtype=float)  # Преобразуем список в массив NumPy
    return num_arr

def iterable_form(arr: np.ndarray):
    '''
    Преобразует матрицу коэффициентов в форму, удобную для итерационного метода.
    
    Args:
        arr (np.ndarray): Входной массив коэффициентов и свободных членов.
    
    Returns:
        tuple: Матрица C, вектор d и норма матрицы C.
    '''
    n = arr.shape[0]  # Получаем количество уравнений
    A = arr[:, :-1]  # Извлекаем матрицу коэффициентов
    b = arr[:, -1]   # Извлекаем вектор свободных членов
    C = np.zeros_like(A, dtype=float)  # Инициализируем матрицу C
    d = np.zeros_like(b, dtype=float)  # Инициализируем вектор d

    for i in range(n):
        if A[i, i] == 0:  # Проверяем, не является ли элемент на главной диагонали нулевым
            raise ValueError("Нулевой элемент на главной диагонали, метод итераций неприменим!")
        
        d[i] = b[i] / A[i, i]  # Вычисляем элементы вектора d
        for j in range(n):
            if i != j:
                C[i, j] = -A[i, j] / A[i, i]  # Заполняем матрицу C

    norm_C = np.linalg.norm(C, ord=np.inf)  # Вычисляем норму матрицы C
    print(f'🔍 Норма матрицы C: {norm_C}')
    
    if norm_C >= 1:  # Проверяем условие сходимости
        print('⚠ Внимание: метод может не сойтись, так как норма C >= 1')
    
    return C, d, norm_C  # Возвращаем матрицу C, вектор d и норму

def simple_iteration(C: np.ndarray, d: np.ndarray, norm_C: float, eps=1e-6, max_iter=1000):
    '''
    Решает систему уравнений методом простых итераций и вычисляет априорную оценку погрешности.
    
    Args:
        C (np.ndarray): Матрица коэффициентов.
        d (np.ndarray): Вектор свободных членов.
        norm_C (float): Норма матрицы C.
        eps (float): Точность решения.
        max_iter (int): Максимальное количество итераций.
    
    Returns:
        np.ndarray: Решение системы уравнений.
    '''
    n = len(d)  # Получаем количество переменных
    x = d.copy()  # Начальное приближение x(0) = d

    for iteration in range(max_iter):
        x_new = np.dot(C, x) + d  # Вычисляем новое приближение
        error = np.linalg.norm(x_new - x, ord=np.inf)  # Вычисляем ошибку

        if error < eps:  # Проверяем, достигнута ли заданная точность
            print(f'✅ Решение найдено за {iteration + 1} итераций')
            return x_new
        
        # Априорная оценка ошибки
        if norm_C < 1:
            apriori_error = (norm_C / (1 - norm_C)) * error
            print(f'🔹 Априорная оценка ошибки на итерации {iteration + 1}: {apriori_error}')
        
        x = x_new  # Обновляем текущее приближение

    print('⚠ Метод не сошелся за максимальное количество итераций')
    return x  # Возвращаем последнее приближение

def solving_equation():
    '''Основная функция для решения уравнений.'''
    arr = get_data()  # Получаем данные от пользователя
    C, d, norm_C = iterable_form(arr)  # Преобразуем данные в удобный формат
    solution = simple_iteration(C, d, norm_C)  # Решаем систему уравнений
    print('\n🔹 Решение системы: ', solution)  # Выводим решение

def main():
    '''Точка входа в программу.'''
    solving_equation()  # Запускаем процесс решения уравнений

if __name__ == '__main__':
    main()  # Запускаем основную функцию